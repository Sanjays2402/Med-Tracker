apiVersion: v1
kind: Service
metadata:
  name: {{ include "med-api.fullname" . }}
  labels:
    {{- include "med-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "med-api.selectorLabels" . | nindent 4 }}
