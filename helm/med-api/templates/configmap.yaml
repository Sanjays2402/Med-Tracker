apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "med-api.fullname" . }}
  labels:
    {{- include "med-api.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
