# Med-Tracker docs

This directory is the source of truth for documentation.

* `architecture/` — system level diagrams and decisions
* `adrs/` — architecture decision records
* `guides/` — how to do things as a contributor
* `api/` — HTTP API reference

The full docs site is rendered with Docusaurus during release.
