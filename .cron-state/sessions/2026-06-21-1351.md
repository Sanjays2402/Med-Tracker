# Tick 12 — 2026-06-21 13:51 PDT

Shipped FIVE features this tick. All clean (no fixup commits).
Second clean tick in a row.

## Commits

- 0cc677e feat(utils): medication-refusal-trend
- fc3f82a feat(utils): followup-overdue-digest
- 13c4a9d feat(utils): lab-window-completion-feed
- 381f648 feat(utils): appointment-prep-text-export
- 75867f3 feat(utils): prescriber-contact-roster-print

## Quality gate

- `pnpm --filter @med/utils test`: 1466/1466 pass (146 new this tick)
  - medication-refusal-trend: 32 tests
  - followup-overdue-digest: 31 tests
  - lab-window-completion-feed: 32 tests
  - appointment-prep-text-export: 26 tests
  - prescriber-contact-roster-print: 25 tests
- `pnpm --filter @med/utils typecheck`: 43 errors (UNCHANGED from
  start-of-tick baseline). Zero new errors from any of the 5 new
  modules.
- `pnpm --filter @med/utils lint`: ok (placeholder)
- `pnpm --filter @med/utils build`: ok (placeholder)
- Workspace `pnpm typecheck`: still fails on `@med/config` baseline
  (missing @types/node). Workspace `pnpm build` hangs on mobile/web
  builds that are baseline-broken per STATE.md — not affected by
  utils-only ticks.

## Theme

Composition tick. Every module composes on at least one prior
module rather than introducing a brand-new domain:

- medication-refusal-trend → medication-refusal-log (tick 11)
- followup-overdue-digest → appointment-followup-tracker (tick 11)
  + caregiver-digest (foundation)
- lab-window-completion-feed → lab-window-tracker (tick 7) +
  appointment-followup-tracker (tick 11)
- appointment-prep-text-export → appointment-prep-checklist (tick 10)
- prescriber-contact-roster-print → prescriber-contact-card (tick 11)

This is the deliberate "ship the foundation, then ship the
composition" pattern: small revertible modules that snap together
at the dashboard layer.

## Notes

- Type naming discipline holding (5/5 modules used module-prefixed
  type names): RefusalTrendDirection, FollowupDigest,
  LabCompletionFeedResult, AppointmentTextExport, ContactRoster.
  Zero collisions, zero fixup commits.
- One dev-time bug caught in lab-window-completion-feed: UTC
  parsing of YYYY-MM-DD strings shifted the draw back one day in
  PDT and broke earliest-wins. Fixed with a local-date parser
  matching appointment-followup-tracker's parseIsoDate so both
  endpoints live in the same local-date space. Lesson: when
  composing on a tracker that uses local-date parsing, the bridge
  module must use the same parser — UTC is a footgun the moment
  midnight is crossed in the user's timezone.
- followup-overdue-digest adopts a NULL short-circuit on silent
  weeks (returns null instead of "everything's fine" body) that
  is NOT in caregiver-digest. Justification: caregivers have inbox
  fatigue. The hasFollowupDigest predicate lets cron callers skip
  the SMTP call entirely.
- Roadmap refilled with Tier 1G (15 candidates, #86-#100).
