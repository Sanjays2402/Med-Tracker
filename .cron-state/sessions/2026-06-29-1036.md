# Tick 53 — 2026-06-29 10:36 PDT

FRONTEND-FOCUS override active. Five Tier 2P slices shipped to apps/web, one
commit each, gated once, pushed to origin/main (959e3ab..dc934d1).

## Shipped

1. **today-expand-undo-burst** (09918bf) — A one-shot sage burst on the section
   headers an "Expand done" tap reopens on /today, pairing tick 52's reopen
   toast (#476) so the eye catches which finished sections came back. New
   `reopenedLabels(groups, set)` in lib/section-collapse-pref returns the done
   labels currently folded that an Expand reopens; `newlyExpandedCount` now
   counts it so the toast count and the burst-set can't drift. The page wraps
   each part-of-day section in a relative container and renders the existing
   reduced-motion-safe `.caught-up-burst` keyframe (#468) on burst-set members;
   burst auto-clears after 900ms. +4 tests (32 in suite).

2. **refills-density-keyboard-help** (5c23154) — The /refills timeline density
   flip (bare "d", #477) now shows in the KeyboardHelp (?) overlay's Actions
   group, but ONLY on /refills, so the cheat sheet mirrors what the page listens
   for. NEW lib/keyboard-shortcuts: BASE_GROUPS + routeActionShortcut(pathname)
   (reuses STRIP_DENSITY_HOTKEY) + shortcutGroupsFor(pathname) which folds the
   route-local row in before the always-last Esc and returns fresh arrays.
   KeyboardHelp derives its groups from usePathname; the inline GROUPS const is
   gone. New 10-test suite.

3. **notifications-favicon-dot-tone** (58c7e68) — The favicon unread dot (#478)
   is now TONED by the worst unread kind: coral for an unread refill / system /
   caregiver alert, amber for a plain dose reminder. favicon-badge gains a
   FaviconBadgeTone ('alert' | 'reminder', default 'alert' to keep existing
   callers coral) + faviconBadgeColor + a toned badgeArt. notification-filter
   gains unreadBadgeTone(items): any non-reminder unread escalates to 'alert',
   only-reminders is 'reminder', nothing is null. The page derives the tone from
   the unsnoozed unread set and passes it into faviconHref. +6 favicon-badge
   tests (19), +6 notification-filter tests (63).

4. **medications-supply-legend-click-filter** (54d5027) — Each supply-legend
   band swatch is now a toggle that filters the list to just that band, with an
   active-band chip ("Showing 2 with under 7 days of supply") + Clear. NEW
   filterBySupplyBand(meds, band, opts) + supplyBandLabel(band, opts) +
   SupplyBand type in lib/days-left-tone, all forwarding cut points to
   buildSupplyBar so filter/bars/counts/labels can't drift. The page computes
   legend counts over the SEARCHED list (pre-filter) so selecting a band doesn't
   zero the other swatches, gates the filter on cfg.showSupplyBar so Clear is
   always reachable, and has a band-specific empty state. +7 tests (46 in suite).

5. **caregivers-bar-percent-aria-sync** (dc934d1) — When the at-risk bar's "Show
   percents" toggle (#480) surfaces each chip's percent as visible text, the
   chip's aria-label dropped the redundant percent so a screen reader no longer
   hears "25%" twice. expirySegmentAriaLabel gains an { includePercent } option
   (default true preserves the self-contained label); when false it leads with
   the kind phrase. The page passes includePercent: !showPercents. The bar-level
   expiryBarAriaDescription still names every percent, so no data is lost. +5
   tests (41 in suite).

## Gate (once, whole batch)

- BUILD: `@med/web` Compiled successfully in 3.4s; all routes prerendered.
- TEST: 1132/1132 across 64 suites (+37, +1 new suite: keyboard-shortcuts 10).
- TYPECHECK: baseline 980 errors (@types/node + React-dup Link) UNCHANGED;
  grep-confirmed ZERO new errors trace to any edited lib or page.
- LINT: `next lint` fails identically at baseline (Next 16 removed it).

## Notes

- All five compose existing tested logic and match the sage/coral/amber pillbox
  language. New logic lives in tested lib/*.ts; pages stay thin renders.
- Refilled Tier 2Q (#486-495) — ten fresh frontend-first candidates.
- Staged only own files per commit (no git add -A) in case a sibling subagent
  runs concurrently.
