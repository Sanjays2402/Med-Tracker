# Tick 33 — 2026-06-25 21:30 PDT

FRONTEND-FOCUS override active (Sanjay, 2026-06-23). Five frontend slices, five
distinct surfaces, each with a tested pure-logic lib/*.ts core.

## Shipped (5)

1. **today-progress-segments** (`6821016`) — /today. Segmented pill row replacing
   the flat progress bar: one segment per dose, sage filled (taken) / hollow
   (pending) / amber (skipped) / coral (missed). Segments scroll their dose row
   into view + pulse. Caption rolls counts. lib/dose-segments.ts, 16 tests.
2. **medication-detail-adherence-ring** (`2557903`) — /medications/[id]. Real
   per-med 30d adherence row (getMedicationAdherence) rendered as the dashboard
   AdherenceRing, replacing the old scaled-7d guess. On-track/slipping/needs-
   attention chip + honest no-data state. lib/med-adherence.ts, 15 tests.
3. **reports-export-format-cards** (`6e104fa`) — /reports/export. Flat list ->
   2-up selectable format cards (CSV/JSON/ICS/PDF) with what's-inside + live
   file-size estimate from real record counts; sticky download bar.
   lib/export-formats.ts, 18 tests.
4. **upcoming-grouped-by-day** (`bf25325`) — /upcoming. Today-only list -> 7-day
   forward projection (composes the month view's dosesForDay) grouped under
   sticky relative-day headers; today's passed times dropped; real Take wired
   for today's rows. lib/upcoming-doses.ts, 16 tests.
5. **caregiver-share-scope-editor** (`119428a`) — /caregivers/new. Grouped scope
   editor (Can see / Can do) with custom sage check controls + live plain-
   language summary and an act-without-view warning. lib/scope-model.ts, 21 tests.

## Gate

- build: `Compiled successfully in 3.3s`, all 60 static pages generated.
- test: 377/377 pass across 25 suites (291 baseline + 86 new). TMPDIR=/Volumes/Projects/.tmp.
- typecheck: zero new errors — verified every apps/web error is documented
  baseline (Link bigint JSX, .next/types validator, DayRail @ts-expect-error,
  packages/icons implicit-any, packages/ui react-not-found placeholder).
- lint: pre-existing `next lint` "Invalid project directory" tooling bug.

23rd clean tick in a row. No fixups, no force-push, no revert. Pushed to
origin/main bc2a9c9..119428a.

## Notes for next tick

- 15 open frontend items remain across Tier 1U/1V/1W (above refill threshold —
  no refill needed yet; will need one when it drops below 5).
- Untouched-but-tempting next surfaces: #307 medications-bulk-archive (reuse
  dose-selection generalised over ids), #297 reports-adherence-bars-window-picker,
  #292 settings-theme-preview, #274 interactions-graph, #277 dashboard-empty-state.
- Pattern holding well: extract logic to lib/*.ts + tests, keep the page a thin
  render. The web vitest harness is now 377 tests across 25 suites.
