# Tick 30 — 2026-06-25 06:28 PDT

FRONTEND-FOCUS override active (Sanjay, 2026-06-23). Third frontend tick.
Shipped 5 frontend slices from Tier 1V + the remaining Tier 1U candidates
(#282, #284, #283, #289, #287), each with a tested pure-logic core. Web test
harness grew from 66 -> 130 tests.

## Shipped (5 commits, all on origin/main)

1. **4e7ed85 — medications-list-search-sort (#282)**
   Inline search box (name/strength/form) + Name / Lowest supply / Soonest
   refill sort chips on /medications, keyboard-focusable, "/" focuses the
   search box. Runout sort divides remainingDoses by a parsed doses-per-day and
   shows a "~Nd left" estimate chip per row. Logic: lib/medication-sort.ts (16).

2. **cb56e2c — refills-timeline-strip (#284)**
   30-day horizontal timeline above the /refills groups: today marker line,
   shaded overdue gutter, weekly gridline ticks, greedy lane-stacking so
   coincident refill dates don't overlap, tone ramp (coral overdue / amber
   within-a-week / sage later / muted ready). Renders only with 2+ plottable
   refills. Component: components/RefillTimeline.tsx. Logic:
   lib/refill-timeline.ts (12).

3. **77b6f77 — adherence-ring-detail-popover (#283)**
   Dashboard Two-week-pulse adherence ring is now a click target opening a
   popover: stacked mini-bar + per-status rows (taken/skipped/missed) with icon
   capsule, count, percent. Closes on outside-click / Escape. Split derived from
   taken-vs-scheduled (every not-taken dose is "missed" — the API doesn't expose
   a skipped split yet; honest + conservative for a health app). Largest-
   remainder rounding so the three percentages sum to exactly 100. Component:
   components/AdherenceBreakdownPopover.tsx. Logic: lib/adherence-breakdown.ts (12).

4. **281d5dd — dose-history-week-strip (#289)**
   Seven-pill week row on the medication detail page: each of the last 7 days'
   adherence state for that med (full=sage / partial=amber / missed=coral /
   none=muted), weekday initial + day number, today ring, summary line
   (% on schedule, perfect days, missed days). Loads 7 days of per-med history
   in a non-blocking effect that degrades to empty days if history is
   unavailable. Component: components/WeekStrip.tsx. Logic: lib/week-strip.ts (12).

5. **9009729 — notifications-filter-tabs (#287)**
   All / Reminders / Refills / System tab row on /notifications with unread-
   aware count badges (sage badge = unread count, muted = total). Filters
   client-side; caregiver notifications fold into System so every item lands
   under exactly one tab. Snooze + mark-read + snoozed-row collapse all keep
   working under the active filter; empty-tab state distinct from empty-inbox.
   Logic: lib/notification-filter.ts (12).

## Gate (run once, end of batch)

- **build**: SUCCEEDS — `Compiled successfully in 3.2s`; all 60 static pages
  generated incl. the 4 edited routes (/dashboard, /medications, /notifications,
  /refills) and /medications/[id]. This is the real render gate.
- **test**: 130/130 across 10 web suites (66 baseline + 64 new) with
  TMPDIR=/Volumes/Projects/.tmp.
- **typecheck**: only the pre-existing baseline — 4 .next/types/validator.ts
  layout-config errors + the packages/utils taper-plan/titration strict-
  undefined baseline. ZERO new errors in any apps/web file or new lib module
  (verified by grepping the tsc output for apps/web + new module names).
- **lint**: pre-existing `next lint` "Invalid project directory" tooling bug,
  unchanged from tick 29. Not introduced by this tick.

## Harness note

Web suite count: 66 -> 130. Five new lib/*.ts modules + co-located tests under
apps/web/tests/. Keep extracting non-trivial logic into lib/ + tests/.

## LSP false positive (recorded so future ticks don't chase it)

Every edited .tsx triggered the stale-@types/react@18.2.79 diagnostic "'Link'
cannot be used as a JSX component … Type 'bigint' is not assignable to type
'ReactNode'". The workspace's real tsc (18.3) reports those files clean.
Verified again this tick. Do not "fix" Link usages — not a real error.

## Next tick

Remaining frontend backlog: 5 Tier 1U stragglers (#274 interactions-graph,
#275 pill-identifier live-preview, #276 caregivers-share-qr, #277 dashboard-
empty-state, #279 reports-monthly-print) + 10 Tier 1V items (#281 today-undo-
toast-stack, #285 schedule-day-drilldown, #286 command-palette-recent,
#288 medication-supply-sparkline, #290-#295). Keep extracting tested logic
into lib/. Backend tiers 1L-1T paused until Sanjay removes the override.
