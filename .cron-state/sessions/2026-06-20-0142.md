# tick 1 — 2026-06-20 01:42 PDT

Branch: `feature/autoship` (created off `main` this tick)

## Shipped

5 vertical feature slices, all pure utilities in `packages/utils`:

| # | feature | sha | tests |
|---|---|---|---|
| 1 | renewal-window — Rx renewal-eligibility planner (payer % consumed, stockout date, refill gap, early-fill flag) | c571a7f | 8 |
| 2 | missed-dose-replan — safe recovery action after a missed dose (take-now / take-now-shift / wait-then-take / skip) with min-interval, halfway-rule, and rolling-window cap guardrails | 017535d | 9 |
| 3 | dose-rounding — round computed target dose to dispensable tablet combos (halves/quarters with max-pieces cap) or step ladders (syringe/pen with min/max/step), enforce safety floor/ceiling and max-deviation flag | 4f6e2df | 14 |
| 4 | streak-rescue — classify streak risk (safe / at-risk / grace-take / makeup-available / broken) with rescue deadlines, plus exported `streakDaysAsOf` helper | 19d5df9 | 10 |
| 5 | pharmacy-hours — pharmacy hours resolver (weekly + per-date overrides + 24h flag), with `pharmaciesOpenNow` triage by closing time | 6e174b1 | 11 |

Bootstrap commit (not counted): ca36e2e — STATE.md + roadmap.

## Gate

- `pnpm --filter @med/utils test` — **305 / 305 pass** (49 new + 256 existing).
- `pnpm --filter @med/utils run typecheck` on JUST the new files — clean.
- Full `pnpm typecheck` — fails on pre-existing baseline issues in
  `@med/config`, `@med/db`, `@med/ui`, and `@med/utils/{taper-plan,titration}.ts`.
  Verified those errors reproduce on `main` itself, no autoship code touched.
  Will track these in STATE.md and not blame future ticks; not pushing red
  code that the autoship loop introduced.
- Pushed batch to origin/feature/autoship after verifying clean log.

## Environment notes

- `/` was at 100% capacity (only 117MiB free on tick start). Cleared
  /tmp scratch (hermes venvs, stale json caches), freeing ~170 MB.
- Reconfigured pnpm to use `/Volumes/Projects/.pnpm-store` so future ticks
  don't fight the root volume for headroom.
- First `pnpm install --prefer-offline --ignore-scripts` took ~11 min
  because the local pnpm store was a separate volume from the project,
  forcing network re-downloads. Subsequent ticks should be near-instant
  since the store now lives on the project volume.
- Repo uses node 20+ per `.nvmrc`; current host is on node 25.9, pnpm 9.12.0.

## Next tick plan

- Ship next 5 from roadmap: notification-batcher, dose-history-aggregator,
  bp-log, weight-trend, glucose-log. All pure utilities, same pattern.
- Consider fixing the pre-existing `@med/ui` React JSX runtime issue
  (probably one line in `packages/ui/vitest.config.ts` adding
  `esbuild: { jsx: 'automatic' }`) so any future UI features have a real
  gate to pass.
