# Tick 15 session — 2026-06-21 23:25 PDT

## Summary
Shipped 5 vertical feature slices to `main` in 35 minutes, all clean.
Fifth consecutive composition tick — every module composes directly
on a tick-14 output.

## Commits (chronological)
1. `e81504f` feat(utils): regimen-snapshot-archive-history-rollup-html
2. `6c7977c` feat(utils): dose-export-csv-import-roundtrip-validator-html
3. `1657c8b` feat(utils): followup-digest-text-html-bundle-i18n
4. `0125aa9` feat(utils): refusal-reason-suggest-i18n-rollup
5. `ac528fe` feat(utils): prescriber-contact-card-emergency-card-pdf

## Quality gate
- `@med/utils`: 1902/1902 tests pass (103 test files).
  + 182 new tests this tick: 35 (rollup-html) + 39 (validator-html) +
    38 (bundle-i18n) + 26 (suggest-rollup) + 44 (emergency-pdf).
- `@med/utils` typecheck: 42 baseline errors across the same 6
  pre-existing files (adherence-risk / date / ics / schedule-resolver
  / taper-plan / titration); ZERO new errors introduced by tick 15.
- `@med/api`: 131/131 pass (first parallel-with-utils run hit 2
  timing-flake timeouts on import-dependent auth tests; isolated re-run
  is clean).
- `@med/ui`: 228/228 fail (pre-existing JSX runtime bug, baseline).
- Lint + build placeholders ok.

## Composition lineage
T11 (foundation) -> T12 (first derivative) -> T13 (second derivative)
-> T14 (third derivative) -> **T15 (fourth derivative)**.

Every T15 module is a direct layer above a T14 output:
- `history-rollup-html`  ←  `regimen-snapshot-archive-history-rollup` (T14)
- `validator-html`        ←  `dose-export-csv-import-roundtrip-validator` (T14)
- `bundle-i18n`           ←  `followup-digest-text-html-bundle` (T14)
- `suggest-i18n-rollup`   ←  `refusal-reason-suggest-i18n` (T14)
- `emergency-card-pdf`    ←  `prescriber-contact-card-emergency-card` (T14)

## Roadmap state
- Tier 1I complete (5/15 from this tick; 10 deferred to later).
- Tier 1J refilled with 15 fresh candidates (#131-#145), all 5th-
  derivative compositions on tick-15 outputs.

## Notes for next tick
- Tier 1J entries `#131-145` are mostly composition-on-T15 patterns
  again. Best next picks: csv-export of history-rollup (parallel to
  dose-export-csv pattern), multi-locale builder for the i18n bundle
  (key-set already proven), two-up PDF variant.
- Module-domain-noun prefix discipline held across all 5 modules.
  No accidental generic-name collisions.
- Five clean ticks in a row — zero fixup commits, zero force-pushes,
  zero reverts.

## Process notes
- LSP diagnostics on saved files were stale in several places this
  tick (after edits, the diagnostics showed old-state errors that
  didn't reflect the new content). Always confirmed by re-running
  `tsc --noEmit -p packages/utils/tsconfig.json` directly. The new-
  file tsc check is the source of truth; don't trust the LSP
  cache for fresh writes.
- `pnpm -r test` parallel race: 5000ms test timeout in
  `@med/api`'s import-heavy auth tests is just enough margin that
  parallel with the @med/utils suite (1902 tests, 4s wall) trips
  intermittent timeouts. Worth raising to 10000ms when @med/api
  gets touched — but NOT in scope for an autoship tick.
- Test file for `regimen-snapshot-archive-history-rollup-html` had
  to be rewritten once: first attempt built `SnapshotPayload`
  directly which has a different shape than the existing
  `buildRegimenSnapshot` helper produces. The right pattern (mirrored
  from the existing rollup test) is to use `buildRegimenSnapshot` +
  `.payload` so all envelope fields stay consistent.
- Followup digest i18n had a plural-form bug in the first draft:
  `opener.dueSoon` / `opener.upcoming` keys used a `follow-up(s)`
  literal that no English speaker would write. Split into `.one`
  and `.many` per key (matching the digest's actual output) to fix.
- HTML opener find-and-replace needed an HTML-escape pass on the
  source string before substitution because the html builder
  `&quot;`-escapes the oldest title — without the escape pass the
  patch missed every HTML opener.
