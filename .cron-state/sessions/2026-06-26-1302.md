# Tick 36 — 2026-06-26 13:02 PDT

Frontend tick #9 under Sanjay's standing FRONTEND-FOCUS override. Shipped 5
vertical UX slices across 5 surfaces, each with its pure-logic core extracted
into a tested lib/*.ts module. One commit per slice, gated once at the end,
pushed to main.

## Shipped

1. **medications-runout-group-persist** (52fbfe7) — `/medications`
   - lib/runout-group-pref.ts (11 tests): storage key + normalize/parse/serialize.
   - The "Group by run-out" toggle now survives a reload (restore on mount,
     write on toggle), parallel to the existing density-pref pattern.

2. **refills-runout-sort** (394c1f9) — `/refills`
   - lib/refill-sort.ts (12 tests): REFILL_SORTS, refillDaysUntil (injectable
     now), sortRefills (overdue-first, nulls-last, name tiebreak),
     summarizeRefillSort.
   - Default / Soonest run-out control beside the status tabs; sort applies
     within each status group so each section surfaces its most urgent rows.

3. **dashboard-adherence-trend-arrow** (35b12aa) — `/dashboard`
   - lib/adherence-trend.ts (12 tests): classifyAdherenceTrend (flat dead-band),
     adherencePercent, trendFromCounts (null when no honest prior baseline).
   - The Two-week pulse trend line now carries a real "+6pp vs prior 30d" delta
     chip + a spelled-out sub-line. AdherenceSummary gained optional
     priorTaken/priorScheduled; the deterministic seed supplies 148/170 (~87%)
     so the current ~93% reads as a genuine +6pp rise — not a fabricated number.

4. **history-streak-callout** (08e0dd2) — `/history`
   - lib/history-streak.ts (13 tests): currentStreak, longestStreak,
     summarizeStreak over the heatmap day series (on-track >= 70%, future days
     skipped, broken streak renders nothing).
   - A sage "N-day streak" callout with a personal-best chip, start date, and
     best-ever length when longer.

5. **schedule-month-density-dots** (cd55c2d) — `/schedule/month`
   - lib/month-density.ts (10 tests): dayLoad buckets, densityDots (cap +
     overflow, clamps negatives/NaN, floors fractions), LOAD_TONE_VAR.
   - The "+N more" cell footer is now a tone-ramped dose-density dot row
     (light sage -> steady -> busy amber -> heavy coral) with a "+N" overflow.

## Gate (run once for the batch)

- BUILD: `@med/web` compiled successfully in 3.2s; all 60 static pages
  generated incl. every edited route.
- TEST: 561/561 pass across 39 suites (503 baseline + 58 new this tick).
  TMPDIR=/Volumes/Projects/.tmp.
- TYPECHECK: ZERO new errors. The 5 new lib + 5 test files appear 0 times in
  tsc output; every error on the 5 edited pages is the documented TS2786
  `Link` bigint baseline (grepped — no non-TS2786 page errors).
- LINT: documented pre-existing `next lint` "Invalid project directory" bug
  (unrelated to this tick; present on baseline).

26th clean tick in a row — no fixup commits, no force-push, no revert.

## Notes / decisions

- All 5 slices are genuinely solid and span 5 distinct surfaces, so I hit the
  full target of 5 without padding.
- #333 (today-group-by-part-of-day) from Tier 1Y was already shipped — the
  /today list already groups Morning/Afternoon/Evening/Night — so I skipped it
  and listed a counts-only follow-up (#344) in Tier 1Z instead.
- Honesty guard on the dashboard trend: trendFromCounts returns null when the
  prior window has no scheduled doses, so the UI falls back to the summary enum
  rather than inventing a delta. The seed's prior-window figures keep the
  shipped "+6pp" consistent with the existing trend: 'up' enum.
- Roadmap refilled with Tier 1Z (#336-#345). Backend tiers 1L-1T stay paused
  until Sanjay removes the override.
