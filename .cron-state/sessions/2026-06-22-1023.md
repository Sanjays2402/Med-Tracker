# Tick 18 session — 2026-06-22 10:23 PDT

## Shipped (5)

| # | SHA | Module | Tests added |
|---|-----|--------|-------------|
| 171 | d7d365a | regimen-snapshot-archive-history-rollup-csv-export-merge-anonymise | 23 |
| 162 | bbe11f9 | dose-export-csv-import-roundtrip-validator-summary-text-slack-thread-batcher | 26 |
| 163 | d5009fb | followup-digest-text-html-bundle-i18n-multi-locale-cron-batcher-html-mailer | 23 |
| 164 | b73cbc6 | refusal-reason-suggest-i18n-rollup-html-print-cover-sheet | 27 |
| 165 | 6bd5a92 | prescriber-contact-card-emergency-card-pdf-two-up-watermark-roster | 25 |

Total new tests: 124. Suite: 2229/2229 in @med/utils.

## Gate

- `pnpm --filter @med/utils test` — 2229/2229 pass.
- `pnpm --filter @med/utils typecheck` — 43 baseline errors in same
  6 pre-existing files (adherence-risk, date, ics, schedule-resolver,
  taper-plan, titration). Zero new errors introduced by tick 18.
- `pnpm --filter @med/utils lint` — placeholder ok.
- `pnpm --filter @med/utils build` — placeholder ok.
- Push: 4753807..6bd5a92 main -> main.

## Composition rhythm

Eight ticks deep (T11 -> T18). Every tick 18 module composes on at
least one T17 output OR a parallel-pattern T17 module:

- merge-anonymise -> csv-export-merge (T16) + parallel to per-class (#151)
- thread-batcher -> summary-text-slack (T17)
- cron-batcher-html-mailer -> cron-batcher (T17)
- html-print-cover-sheet -> html-print (T17)
- watermark-roster -> two-up-watermark (T17)

## Firsts

- **First PHI-safe export companion**: anonymise uses HMAC-SHA-256
  via Web Crypto (matching caregiver-share-token + regimen-snapshot-
  archive). Three name strategies. Hash truncation 4-64. Collision
  detection. HIPAA safe-harbor framing for the columns that AREN'T
  touched.
- **First Slack thread-shape module**: thread-batcher follows the
  parent+replies pattern for daily QA digests. Two API calls per
  day regardless of N runs.
- **First SMTP envelope wrapper**: html-mailer produces the
  to+subject+text+html shape mailer queues consume directly. Maps
  1:1 to Nodemailer / AWS SES / Sendgrid.
- **First cover-sheet pattern**: html-print-cover-sheet sets the
  template for future validator-html-print-cover-sheet,
  prescriber-contact-card-emergency-card-pdf-binder-cover, and the
  multi-patient batch variant in Tier 1M.
- **First per-page traceability strip**: roster header strip with
  5-token template (pageNumber, totalPages, batchId, watermarkText,
  generatedAtLabel) and djb2 deterministic batchId. Sets the
  pattern for future multi-page roster modules (trifold, TOC, etc).

## Roadmap

Marked done: #162, #163, #164, #165, #171 (all 5 tier 1L items
attempted; remaining tier 1L items #161, #166-#170, #172-#175 stay
on deck). Refilled Tier 1M with 15 new candidates (#176-#190),
each chained 6th-derivative or further off the same five spines.

## Discipline

- Eighth clean tick. No fixup commits. No force-push. No revert.
- Every commit revertible in isolation.
- Every commit has its own test suite.
- Every commit passes the full @med/utils gate in isolation AND
  in batch.
- Module-domain-noun export naming on every public export this tick.
- Hardware corner cases: HMAC short-secret guard, https:// URL
  gating on Slack dashboard button (matching per-run renderer),
  HTML escaping on user-controlled labels, date locking across
  multi-page batches to prevent midnight-rollover mixed-date stacks.

## Skips this tick

None. All 5 features genuinely solid.

## Notes for next tick

- Roadmap Tier 1M is the next 15 items; ~9 items still queued in
  Tier 1L untouched (#161, #166, #167, #168, #169, #170, #172, #173,
  #174, #175). Composition options continue to be richer than the
  ticking pace can drain.
- Auto-batchId hash function is djb2 (non-cryptographic) — fine for
  the audit-trail use case but if a future use surfaces where the
  batchId needs to be hard to predict (e.g. for a shared URL slot),
  switch to the same Web Crypto path the anonymise module uses.
- The cover sheet and roster header strip are the two new chrome
  patterns this tick. Future "batch" / "binder" / "packet" modules
  should reuse both (cover sheet at front, strip on every page).
