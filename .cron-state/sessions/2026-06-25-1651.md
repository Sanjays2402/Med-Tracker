# Tick 32 — 2026-06-25 16:51 PDT

Fifth frontend tick under Sanjay's standing FRONTEND-FOCUS override. Five
clean slices, five distinct surfaces, each with its pure-logic core in a
tested lib/*.ts module. 22nd clean tick in a row.

## Shipped (5)

1. **today-overdue-banner** (`0b94e6f`) — sticky danger banner on /today when
   1+ pending doses slip past a 15-min grace; jump-to-first smooth-scrolls
   (reduced-motion aware) to the longest-waiting dose and pulses it.
   - `lib/overdue.ts` (19 tests): partition, minutesLate, worst-late, headline
     + lateness formatting, injected `now`.
2. **dashboard-next-dose-countdown** (`060b5df`) — live card between the day
   rail and stat capsules, self-ticking once a minute; accent/amber/coral tone
   with "Nh Nm late" overdue + calm "all caught up"; quick Take wired in.
   - `lib/countdown.ts` (17 tests): composes lib/next-dose; duration split,
     until/since/bare humanised phrasing, clock label.
   - `components/NextDoseCountdown.tsx`.
3. **refills-status-filter-tabs** (`d8a04dd`) — All/Needed/Requested/Ready tabs
   with count badges; lands on most-actionable non-empty tab, respects explicit
   pick; timeline shows only on All.
   - `lib/refill-filter.ts` (14 tests): status->tab bucketing (picked_up folds
     into Ready), counts, default-tab.
4. **notifications-group-by-day** (`e8a8f58`) — day sections with sticky
   relative-day headers + per-group counts, on top of the filter tabs + snooze.
   - `lib/day-group.ts` (16 tests): local-day keying (no UTC drift), whole-day
     delta, relative labels incl. Tomorrow, generic groupByDay.
5. **schedule-week-today-column** (`82aa05d`) — sage spine + accent header +
   "Today" cap on the current weekday column, scroll-into-view on mount (only
   when the week contains today); refreshed column chrome to sage/coral tokens.
   - `lib/week-days.ts` (9 tests): startOfWeek, same-local-day, 7-cell week
     model with today index + containsToday guard.

## Gate

- BUILD: `Compiled successfully in 3.3s`, all 60 static pages (incl. all 5
  edited routes).
- TEST: 291/291 across 20 suites (216 baseline + 75 new) with
  TMPDIR=/Volumes/Projects/.tmp.
- TYPECHECK: only the documented baseline (DayRail unused @ts-expect-error +
  4 .next/types/validator.ts layout errors + packages/utils schedule-resolver/
  taper-plan/titration). ZERO new errors in any touched/created file (verified
  by grepping the tsc output for every path — no matches).
- LINT: documented pre-existing `next lint` "Invalid project directory" bug;
  not introduced here.

## Notes

- The LSP `Link cannot be used as a JSX component / bigint` warning fires on
  every edited .tsx (stale @types/react@18.2.79 in the LSP only). Real tsc is
  clean. Do not chase it.
- Did NOT touch backend tiers 1L-1T (paused under the override).
- Backlog: 20 open frontend items remain in the 266-310 range — above the
  refill threshold, so no roadmap refill this tick.
- Each pure-logic module composes cleanly with existing ones: countdown reuses
  next-dose, refill-filter parallels notification-filter, day-group is generic
  enough to also feed the future /upcoming grouping (#294).
