# Tick 29 — 2026-06-25 00:30 PDT

FRONTEND-FOCUS override active (Sanjay, 2026-06-23). Second frontend tick.
Shipped 5 frontend slices from Tier 1U (#271, #272, #273, #278, #280), each
with a tested pure-logic core. Stood up the FIRST web-app test harness.

## Shipped (5 commits, all on origin/main)

1. **a864074 — today-page-bulk-take (#271)**
   Multi-select mode on /today: Select toolbar + per-row checkboxes, shift+click
   inclusive range select against the ordered pending list, select-all, a sage
   floating action bar with a live count, Promise.allSettled bulk take that keeps
   rows pending on partial failure, one-shot bulk Undo toast. Selection auto-prunes
   when the dose list refreshes. Logic: lib/dose-selection.ts (17 tests).

2. **9093e7a — medications-detail-cover (#272)**
   Gradient hero band on /medications/[id]: large rounded pill glyph, serif name +
   strength, capsules for form / schedule / live next-dose countdown chip (tone by
   urgency). Instructions moved into a hover-reveal inline textarea editor (Save via
   updateMedication, optimistic). Archive hoisted into the hero (desktop) / strip
   (mobile). Logic: lib/next-dose.ts (14 tests).

3. **4faa31b — refill-bottle-progress (#273)**
   Vertical prescription-bottle SVG per refill row: sage liquid fills proportional
   to supply on hand (days-to-refill capped at day supply), eases on change, turns
   coral at/under the medication's refillThresholdDays (20% fallback), muted when
   empty. Refills page now loads medications for real thresholds. Logic:
   lib/bottle-fill.ts (11 tests).

4. **7ae21de — schedule-month-view (#278)**
   New /schedule/month: canonical 6x7 grid anchored on the 1st, dimmed spill days,
   today ring + filled date badge, per-day dose count + up to 3 med chips with
   overflow, day links to history/today/upcoming by date, prev/next month paging
   with year rollover + Today snap, week<->month cross links. Logic:
   lib/month-grid.ts (14 tests, all local-time keys — no UTC drift).

5. **6afaca8 — notifications-snooze-row (#280)**
   Per-row Snooze popover on /notifications (1h / 3h / this evening / tomorrow /
   Monday), each showing its resurface time. Optimistically collapses the row,
   marks read, fires an Undo toast; header shows "N snoozed"; popover closes on
   outside-click / Escape, role=menu + aria. New snoozeNotification data fn +
   snoozedUntil on NotificationItem. Logic: lib/snooze.ts (12 tests).

## Gate (run once, end of batch)

- **build**: SUCCEEDS — `Compiled successfully in 4.0s`; all routes incl.
  /schedule/month prerender. This is the real render gate.
- **typecheck**: only pre-existing baseline — DayRail.tsx(216,9) unused
  @ts-expect-error + 4 .next/types/validator.ts layout-config errors. Zero new.
- **test**: 66/66 across 5 new web suites (TMPDIR=/Volumes/Projects/.tmp).
- **lint**: pre-existing `next lint` "Invalid project directory" tooling bug,
  proven identical via `git stash`. Not introduced by this tick.

## Harness note

apps/web had ZERO test files before today. Tick 29 added apps/web/tests/ with 5
suites wired to the new lib/*.ts logic modules. Future web ticks should keep
extracting non-trivial logic into lib/ + tests/.

## LSP false positive (recorded so future ticks don't chase it)

Every edited .tsx triggered an LSP diagnostic "'Link' cannot be used as a JSX
component … Type 'bigint' is not assignable to type 'ReactNode'". This comes from
a stale `@types/react@18.2.79` the LSP resolves; the workspace's real tsc uses
@types/react 18.3 and reports the files clean. Verified per-file by grepping the
actual `tsc --noEmit` output. Do not "fix" Link usages — it's not a real error.

## Next tick

Pick 5 frontend slices from the remaining Tier 1U (#274 interactions-graph,
#275 pill-identifier live-preview, #276 caregivers-share-qr,
#277 dashboard-empty-state, #279 reports-monthly-print) and/or the fresh
Tier 1V candidates (#281-#295). Keep extracting tested logic into lib/.
