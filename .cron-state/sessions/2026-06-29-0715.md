# Tick 52 — 2026-06-29 07:15 PDT

FRONTEND-FOCUS override active. Five Tier 2O slices shipped to apps/web, one
commit each, gated once, pushed to origin/main (21dd038..d41e2e1).

## Shipped

1. **today-fold-toast-expand-undo** (363ef9e) — Undo toast + re-fold when
   "Expand done" reopens N finished /today sections, mirroring tick 51's
   Collapse-done toast (#471). `newlyExpandedCount` (done sections currently
   folded; zero when the next tap folds instead) + `expandedToastTitle` in
   lib/section-collapse-pref; the page picks the matching toast via `??`. The
   two counts are mutually exclusive per tap. +6 tests (28 in suite).

2. **refills-density-keyboard** (39005fa) — bare "d" flips the /refills
   timeline strip density without the mouse, mirroring the medications "s"
   sort-cycle. `STRIP_DENSITY_HOTKEY` + `isStripDensityHotkey` (modifier-aware,
   case-insensitive) in lib/refill-timeline-density; the RefillTimeline effect
   gates on text-field focus, and a discoverable "d" kbd chip sits by the
   toggle. +4 tests (23 in suite).

3. **notifications-title-favicon-dot** (e3b894e) — coral favicon badge dot
   when unread > 0, pairing the unread tab title (#473). NEW lib/favicon-badge:
   pure SVG-data-URI builder (no canvas, no network) mirroring public/favicon.svg
   art; `hasFaviconBadge` gates on unread > 0 AND not save-data (reduced-data
   hint), `faviconHref` falls back to the static path otherwise. The page
   finds-or-creates <link rel="icon">, swaps href, restores on unmount. New
   13-test suite.

4. **medications-supply-legend-counts** (3df6195) — per-band count appended to
   the supply legend ("under 7d · 2"), counting only the visible bar-drawing
   meds so the tally tracks search narrowing. `supplyLegendCounts` in
   lib/days-left-tone composes buildSupplyBar so labels+bars+counts stay in
   lock-step. +4 tests (39 in suite).

5. **caregivers-bar-percent-labels** (d41e2e1) — inline largest-remainder
   percent on each at-risk legend chip behind a persisted "Show percents"
   toggle. `segmentPercentLabel` in lib/expiry-bar (same width the bar draws;
   chips sum 100) + NEW lib/expiry-bar-percent-pref (persistence guard parallel
   to expiry-bar-pref). +3 expiry-bar tests + new 9-test pref suite.

## Gate (once, whole batch)

- BUILD: `@med/web` Compiled successfully in 3.6s; all routes prerendered.
- TEST: 1095/1095 across 63 suites (+39, +2 new suites: favicon-badge 13,
  expiry-bar-percent-pref 9).
- TYPECHECK: baseline 980 errors (@types/node + React-dup) UNCHANGED;
  grep-confirmed ZERO new errors trace to any edited lib or page.
- LINT: `next lint` fails identically at baseline (Next 16 removed it).

## Notes for next tick

- Tier 2P refilled (#481-485): today-expand-undo-burst, refills-density-keyboard-
  help (wire "d" into the ? overlay), notifications-favicon-dot-tone (tone the
  badge by worst unread kind), medications-supply-legend-click-filter,
  caregivers-bar-percent-aria-sync.
- favicon-badge is generic; #483 (dot-tone) can extend faviconHref with a tone
  param keyed off the worst unread NotificationItem kind.
- A sibling subagent may run concurrently on overlapping test files; staged only
  own files per commit (no `git add -A`) to keep slices clean.
