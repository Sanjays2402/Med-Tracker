# Tick 51 — 2026-06-29 03:55 PDT (FRONTEND-FOCUS override active)

Shipped 5 frontend slices, one commit each, gated once, pushed to main.

## Features
1. 5930015 today-collapse-all-toast — "Collapse done" newly folds N sections ->
   info toast "Folded 3 finished sections" + Undo restoring the exact prior
   collapse set; un-fold tap fires nothing. newlyFoldedCount + foldedToastTitle.
2. 41a47a6 refills-density-pref-settings — refill timeline density now lives on
   /settings/preferences (radio cards) alongside the inline strip toggle; same
   STRIP_DENSITY_STORAGE_KEY. STRIP_DENSITY_OPTIONS descriptions + stripDensityDescription.
3. b02b132 notifications-unread-count-title — unread total in browser tab title
   "(3) Notifications" (restored on unmount) + header pill; capped 99+.
   notificationsTitle + unreadCountPill + totalUnread.
4. 8080fb6 medications-supply-bar-legend — ok/warn/danger colour key under the
   list when inline supply bars are on screen. supplyLegend, bands forwarded so
   labels never drift from bars; only shown when a bar exists.
5. 3a92dbd caregivers-expiry-bar-count-pills — toned per-segment count pills on
   the at-risk legend chips, parallel to tick 50's all-good pill. segmentCountPill.

## Gate
- build: SUCCEEDS (Compiled in 4.0s, all routes prerendered)
- test: 1056/1056 across 61 suites (+15: section-collapse 22, refill-timeline 19,
  notification-filter 57, days-left-tone 35, expiry-bar 34)
- typecheck: 980 baseline (@types/node + React-dup) UNCHANGED, zero new from tick
- lint: next-lint baseline failure (Next 16 removed it) UNCHANGED

## Notes
- All 5 surfaces touched: today, refills/settings, notifications, medications, caregivers.
- Two new helpers + three a11y/UX/micro-interaction layers, every one tested.
- A sibling subagent ran concurrently on overlapping test files (notification-filter,
  days-left-tone, expiry-bar, refill-timeline-density); staged only own files per
  commit to keep slices clean. Refilled Tier 2O (#476-480).
