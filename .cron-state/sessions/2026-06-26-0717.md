# Tick 35 — 2026-06-26 07:17 PDT

FRONTEND-FOCUS override active (Sanjay, 2026-06-23). Eighth frontend tick.
Shipped 5 vertical slices across 5 surfaces, one commit each, gated once.

## Shipped

1. **reports-window-picker-shared** (165d295) — Lifted the 7/30/90d adherence
   window picker into a shared `WindowPicker` component used by /reports and
   /reports/adherence. One control, aria-pressed, Left/Right keyboard cycling.
   Extended `lib/adherence-window` with WINDOW_KEYS, isWindowKey,
   windowKeyForDays, cycleWindow (wraparound). /reports/adherence migrated off
   its raw 7|30|90 number state. tests 7 -> 15.

2. **caregivers-search-filter** (ee93839) — Inline search box on /caregivers
   matching label OR any scope (both raw token and friendly scopeLabel, so
   "refills" hits request-refill). Composes filter -> sort; "/" focus shortcut,
   clear chip, "N of M" match count, dedicated no-match empty state.
   `lib/caregiver-filter.ts` + 11 tests.

3. **command-palette-section-counts** (435ee30) — Per-section result count chip
   in palette headers while querying; never on the curated Recent section.
   `lib/section-count.ts` (shouldShowCount, countLabel, totalResultCount,
   resultsSummary) + 13 tests.

4. **schedule-day-drilldown-empty-jump** (7c93ff8) — Empty-day drilldown offers
   a "Jump to Tomorrow" / "Jump ahead N days" button that scans forward up to
   14 days for the next dosed day (reuses dosesForDay; bounded loop).
   `lib/day-jump.ts` (findNextDayWithDoses, nextDayWithDoses, jumpLabel) + 12 tests.

5. **medications-list-runout-group** (6f1aa33) — Optional "Group by run-out"
   toggle filing meds under urgency bands (Out of supply / This week / This
   month / Healthy / No supply data) with sticky headers, tone dots, counts,
   hints. Row markup extracted into a shared MedRow. `lib/runout-group.ts`
   (runoutBand, groupByRunout, summarizeRunout) + 13 tests.

## Gate (run once for the batch)

- BUILD: `@med/web` Compiled successfully in 3.3s; 60 static pages generated.
- TEST: 503/503 across 34 suites (446 baseline + 57 new) with TMPDIR=/Volumes/Projects/.tmp.
- TYPECHECK: zero new errors in any touched apps/web file; the 5 new lib modules
  appear 0 times in tsc output. All remaining errors are the documented baseline
  (Link bigint JSX, packages/utils, packages/ui placeholders).
- LINT: documented pre-existing `next lint` "Invalid project directory" bug.

25th clean tick in a row. No fixup commits, no force-push, no revert.

## Roadmap

Marked #311/#313/#314/#315/#317 done. Refilled with Tier 1Y (#321-#335,
15 fresh frontend candidates). Open Tier 1X stragglers remain: #312, #316,
#318, #319, #320.
