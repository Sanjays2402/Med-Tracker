# tick 22 — 2026-06-22 23:54 PDT (session note written 2026-06-23 00:08 PDT)

Ships 5 features in `@med/utils`, twelfth clean tick in a row.

## Commits (oldest first)

1. **41a5d93** `regimen-snapshot-archive-history-rollup-csv-export-merge-anonymise-key-rotate-cli-summary` — Fixed-shape single-line CLI summary for an anonymise key-rotation result: `tag patients=N reshuffled=N collisions=N verdict=V`. Verdicts no-op / widen-hash / ship-safe / empty-cohort. Counts reshuffles by walking the mappings (id OR name change counts), counts collisions in both old + new epochs (each member beyond the first counts). `detectAnonymiseKeyRotateCliWarning` returns the most actionable warning string. `summarizeAnonymiseKeyRotationBatchForCli` rolls N cohorts into one `key-rotate-batch` line. Composes on key-rotate (T19). 24 tests.

2. **ff031fb** `dose-export-csv-import-roundtrip-validator-summary-text-slack-thread-batcher-quiet-hours-snooze` — Per-call snooze override for the quiet-hours deferral. Incident-response weekends need the on-call to receive the midnight unread badge; this module accepts `snoozeUntil` OR a `snoozeForMs` convenience and, when the underlying decision is `defer-until` or `suppress-completely` AND `runAt < snoozeUntil`, overrides to `post-now` with reason `snooze-override`. Tags the parent fallback with `(snooze override during {windowLabel} until {snoozeUntil})`. Does NOT touch `post-now` decisions. Composes on quiet-hours (T19). 23 tests.

3. **849da42** `followup-digest-text-html-bundle-i18n-multi-locale-cron-batcher-html-mailer-bcc-suppress-self-cc` — Cross-envelope self-CC suppression for the BCC overlay. Existing `dropPrimaryFromBcc` handles the own-envelope case but not the cross-envelope case (admin gets primary on their own envelope AND BCC on alice/bob/carol's envelopes — 4 copies). Default `suppress-when-primary-elsewhere` strips a BCC address from every other envelope when the address appears as a primary anywhere in the batch. `preserve-all` opts out. `preserveAddresses` list overrides per-address. Recomputes fanOutByAddress to reflect post-suppression delivery counts. Composes on bcc (T19). 17 tests.

4. **4072e06** `refusal-reason-suggest-i18n-rollup-html-print-cover-sheet-binder-spine-color-coding` — Per-source color-coded stripe + verbal tag for the binder spine. Default 4mm color stripe at top of spine, color-keyed to dominant refusal source: NPO-window red, prescriber-pause blue, out-of-supply orange, sleeping-window purple, recent-pattern yellow, no-dominant gray. Verbal tag (NPO/PAUSE/SUPPLY/SLEEP/PATTERN) accompanies stripe so signal degrades gracefully on monochrome printers and for color-vision-different clinicians. `monochromeFallback=true` opts out of stripe entirely. `stripePlacement` supports top/bottom/left/right. `pickDominantSource` ties-break by clinical priority order. Composes on binder-spine (T19) + i18n-rollup coverage struct (T14). 33 tests.

5. **7e57774** `prescriber-contact-card-emergency-card-pdf-two-up-watermark-roster-toc-grouped-html` — Screen-first companion to the flat TOC HTML: renders each specialty group as a native HTML `<details>/<summary>` so the patient portal / household admin can collapse specialties. No JavaScript. Default `defaultGroupState='open'`; `'collapsed'` switches to accordion. Per-specialty `collapsedSpecialties`/`openSpecialties` with `openSpecialties` winning on conflict. Case-insensitive specialty matching. `forceOpenInPrint=true` (default) emits a `@media print` stylesheet that forces all groups OPEN under print. Composes on roster-toc-html (T20). 28 tests.

## Quality gate

- **Tests:** 2792/2792 pass in `@med/utils` (138 test files; 125 new this tick: 24+23+17+33+28).
- **Typecheck:** 43 errors across the SAME 6 pre-existing files (adherence-risk, date, ics, schedule-resolver, taper-plan, titration) — identical to start-of-tick baseline. **Zero new errors** introduced by tick 22.
- **Lint:** placeholder ok.
- **Build:** placeholder ok.

## Composition rhythm

Twelfth composition tick in a row (T11 → T22). Every tick 22 module composes on at least one tick 19/20/21 output:
- cli-summary on key-rotate (T19)
- quiet-hours-snooze on quiet-hours (T19)
- bcc-suppress-self-cc on bcc (T19)
- binder-spine-color-coding on binder-spine (T19) + i18n-rollup coverage struct (T14)
- roster-toc-grouped-html on roster-toc (T19) + roster-toc-html (T20)

## Pattern continuity

- 12 clean ticks in a row (no fixup commits, no force-push, no revert).
- Every commit revertible in isolation; every commit has its own test suite.
- Module-domain-noun prefix discipline maintained across all 5 exports (no generic names like `CliSummary`, `SnoozeResult`, `ColorCoding`, `GroupedHtmlResult` — every type prefixed with its module domain).

## Roadmap

10 unstarted items remain in the fresh Tier 1O (#206-#215). 105 unstarted total across all tiers (older recycled candidates included). No roadmap refill needed this tick — the 5-per-tick target is comfortably above the < 5 refill threshold.
