# Tick 42 — 2026-06-27 13:13 PDT

FRONTEND-FOCUS override active (Sanjay, 2026-06-23). Five frontend slices, one
commit each, spanning five surfaces. Gate run once at the end of the batch.

## Shipped

1. **today-roll-percent-tone** (#390) — `ebc1c5c`
   - NEW `lib/progress-tone.ts`: thirds completion-tone classifier (danger <34,
     warn 34-66, ok 67+) + `progressToneVar` CSS-var map. 10 tests.
   - /today day-roll line tints its "N% done" prefix by that tone.
2. **caregivers-expiry-summary-bar** (#391) — `fc86698`
   - NEW `lib/expiry-bar.ts`: stacked active/soon/expired segment model off
     `summarizeExpiry`; largest-remainder rounding so whole-percent widths sum to
     exactly 100; empty buckets drop; tones match the row pills. 10 tests.
   - 2px bar + dotted legend under the /caregivers header, only when at risk.
3. **dashboard-today-percent-chip** (#387) — `4e78f1f`
   - `dayPercentChip()` added to `lib/day-progress-roll.ts` (percent + label +
     tone via progress-tone; "All done" when complete, null for an empty day).
     +4 tests (14 -> 18).
   - Toned capsule on the dashboard Up-next header. Composes slice 1.
4. **notifications-unread-only-toggle** (#396, also retires Tier 1Y #328) — `129e189`
   - `isUnread` / `filterUnreadOnly` / `applyNotificationFilters` (tab AND
     unread-only in one pass) / `summarizeUnread` added to
     `lib/notification-filter.ts`. +11 tests (12 -> 23).
   - "Unread only" toggle beside the tabs, shown only when the active tab has
     read rows to hide; empty state + "Show all" reset for the hides-all case.
5. **refills-empty-tab-soonest-tone** (#388) — `33b5358`
   - `EmptyTabHint` gains `tone` + `urgent` mirroring the run-out chip tone
     (overdue/<=3d -> danger, else warn). +2 tests (refill-sort 39 -> 41).
   - /refills empty-tab state: toned run-out Pill + danger/warn-tinted icon.

## Gate (run once for the batch)

- **test**: `@med/web` 835/835 pass across 56 suites (+37 new, +2 suites).
- **build**: `Compiled successfully in 3.3s`; all static pages generated incl.
  the 5 edited routes.
- **typecheck**: baseline 980 errors UNCHANGED (documented app-wide React-18
  Link/ReactNode drift); grep-confirmed ZERO errors trace to any new/edited
  lib, test, or page.
- **lint**: no-op (Next 16 removed `next lint`; no ESLint config; empty turbo
  `lint` task) — documented baseline, same as ticks 38-41.

## Push

`56ba153..33b5358 main -> main`, verified `origin/main` 0/0 ahead-behind.
THIRTY-SECOND clean tick in a row.

## Notes for next tick

- Tier 2F (#397-#410) freshly refilled. Several compose THIS tick's new modules:
  #397/#402 reuse progress-tone, #399/#404 extend expiry-bar, #400 persists the
  unread-only toggle, #401 announces the toned empty-tab hint.
- Tier 2E stragglers still open: #386, #389, #392-#395 (mostly aria-live polish).
- Backend tiers 1L-1T stay PAUSED until Sanjay removes the FRONTEND-FOCUS override.
- Baseline still red on @med/config, @med/db, @med/ui, @med/utils typecheck +
  @med/ui test (pre-autoship; documented in STATE.md repo-state notes). The web
  gate (test + build + zero-new-typecheck) is what protects main for this loop.
