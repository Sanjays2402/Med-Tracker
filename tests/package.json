{
  "name": "@med/tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:unit": "vitest run",
    "test": "vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "vitest": "^3.2.6"
  }
}
