{
  "name": "@med/utils",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": { "lint": "echo ok", "typecheck": "tsc --noEmit -p tsconfig.json", "test": "vitest run", "build": "echo ok" },
  "devDependencies": { "vitest": "^3.2.6" }
}
